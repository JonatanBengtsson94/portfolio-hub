function Experience(props) {
  return (
    <div>
      <h4>{props.title}</h4>
      <h5>{props.orginization}</h5>
      <h5>{props.date}</h5>
      <p>{props.description}</p>
    </div>
  )
}

export default Experience
